# from rest_framework.routers import DefaultRouter
# from apps.user_management.roles_views import UnifiedRoleViewSet
# from apps.client_management.views import ClientViewSet
# # from apps.optionlists.views import OptionListViewSet, OptionListItemViewSet
# from apps.cultural_groups.views import CulturalGroupViewSet
# from apps.audit.views import AuditEventViewSet
# from apps.core.views import ReminderTypeViewSet, TermTranslationViewSet
# from api.azure_user_views import AzureUserViewSet

# legacy_router = DefaultRouter()
# legacy_router.register(r'roles', UnifiedRoleViewSet, basename='role')
# legacy_router.register(r'clients', ClientViewSet, basename='client')
# legacy_router.register(r'cultural-groups', CulturalGroupViewSet, basename='cultural-group')
# # legacy_router.register(r'optionlists', OptionListViewSet, basename='optionlist')
# # legacy_router.register(r'optionlistitems', OptionListItemViewSet, basename='optionlistitem')
# legacy_router.register(r'azure-user', AzureUserViewSet, basename='azure-user')
# legacy_router.register(r'audit-events', AuditEventViewSet, basename='audit-event')
# legacy_router.register(r'remindertypes', ReminderTypeViewSet, basename='remindertype')
# legacy_router.register(r'termtranslations', TermTranslationViewSet, basename='termtranslation')

# legacy_urlpatterns = legacy_router.urls
