# Task ID: 1
# Title: Update config/urls.py for User Endpoints Standardization
# Status: pending
# Dependencies: None
# Priority: high
# Description: Modify the main URL configuration to standardize user endpoints with /v1/users/ prefix
# Details:
Update config/urls.py to include user endpoints under /v1/users/ prefix. Modify the urlpatterns to include: path('v1/users/', include('apps.users.api', namespace='users')). Ensure proper namespace configuration and verify that all existing user endpoints will be accessible under the new prefix structure.

# Test Strategy:
Verify URL routing works correctly by testing endpoint accessibility. Create unit tests to ensure all user endpoints respond correctly under the new /v1/users/ prefix. Test URL reverse lookups and namespace resolution.
