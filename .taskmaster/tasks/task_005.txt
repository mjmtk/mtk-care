# Task ID: 5
# Title: Add Roles Router to URL Configuration
# Status: pending
# Dependencies: 4
# Priority: medium
# Description: Update config/urls.py to include the new roles_router with /v1/roles/ prefix
# Details:
In config/urls.py, add a new URL pattern for the roles router: path('v1/roles/', include('apps.users.api.roles_router')). Ensure proper import of the roles_router from apps.users.api. Configure appropriate namespace if needed. Verify the URL pattern is correctly positioned in the urlpatterns list.

# Test Strategy:
Test that role endpoints are accessible under the new /v1/roles/ prefix. Verify URL routing works correctly and endpoints return expected responses. Test namespace resolution and URL reverse lookups for role endpoints.
